// VariationsInfoForm.js
import React from 'react';
import Input from 'components/Input';

const VariationsInfoForm = ({ data, onDataChange }) => {
  // Renderize os campos de entrada com base nos dados do JSON
  // Atualize os dados e chame onDataChange quando os campos mudarem

  return (
    <div className="Variations-info-form">
      {/* Renderize os campos de entrada aqui */}
    </div>
  );
};

export default VariationsInfoForm;