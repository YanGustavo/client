import React from 'react';
import {ProductCard, CardBody, Title, CardTag, CardText, NewPrice, OldPrice, AddToCartButton, Button, Attribution, Desktop, Mobile} from './styles';
import useAnimate from "./useAnimate";


const ProductPreview = ({children}: { children: React.ReactNode }) => {
  useAnimate(); 
  return (
    <>
    </>
  );
}
export default ProductPreview;